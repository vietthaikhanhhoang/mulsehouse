class dataHtml {
  late String content;
  late String type;
  dataHtml(String content, String type) {
    print("content: " + content + " | type: " + type);
    this.content = content;
    this.type = type;
  }
}